import type { ComponentStyleConfig } from '@chakra-ui/react'

export const SpinnerStyle: ComponentStyleConfig = {
  baseStyle: {
    borderColor: 'white',
    width: '100px',
    height: '100px',
  },
}
